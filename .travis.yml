language: java
before_install:
- cd back
- chmod +x gradlew
# whitelist
branches:
  only:
    - master
    - continuous-deployment
deploy:
  provider: script
  skip_cleanup: true
  script: bash deploy.sh $ec2-key $ec2-ip
  on:
    branch: continuous-deployment
